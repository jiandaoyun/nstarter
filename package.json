{
  "name": "nstarter-rabbitmq",
  "version": "0.2.1-beta",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": ""
  },
  "scripts": {
    "build": "rimraf dist && tsc --build tsconfig.json",
    "test": "nyc mocha --config .mocharc.yml --unhandled-rejections=none",
    "eslint": "eslint --ext .js,.ts --config .eslintrc.js ./src",
    "eslint:html": "npm run eslint -- --output-file ./lint/eslint.html --format html",
    "clean": "rimraf coverage && rimraf .nyc_output"
  },
  "main": "./dist",
  "types": "./dist",
  "dependencies": {
    "amqp-connection-manager": "^3.2.0",
    "amqplib": "^0.5.5",
    "async-retry": "^1.3.1"
  },
  "devDependencies": {
    "@types/amqp-connection-manager": "^2.0.8",
    "@types/amqplib": "^0.5.13",
    "@types/async-retry": "^1.4.2",
    "@types/mocha": "^7.0.2",
    "@types/node": "^12.12.34",
    "chai": "^4.2.0",
    "eslint": "^6.8.0",
    "eslint-config-nstarter": "^1.0.5",
    "mocha": "^7.2.0",
    "nstarter-tsconfig": "^0.1.3",
    "nyc": "^15.0.1",
    "rimraf": "^3.0.2",
    "source-map-support": "^0.5.19",
    "ts-node": "^8.10.2",
    "typescript": "^3.8.3"
  }
}
